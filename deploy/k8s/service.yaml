apiVersion: v1
kind: Service
metadata:
  name: pulse-service
  namespace: production
  labels:
    app: pulse-service
spec:
  type: ClusterIP
  selector:
    app: pulse-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3015
